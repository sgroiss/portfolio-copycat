---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Kontakt">
  <main class="container mx-auto py-32 text-white flex-1">
    <h1 class="text-5xl mb-8">Kontakt</h1>

    <p class="leading-relaxed max-w-2xl mb-16">
      Du kannst mich jederzeit über dieses Formular oder direkt per E-Mail
      erreichen.
    </p>

    <form class="max-w-xl space-y-6">
      <div class="flex flex-col">
        <label class="mb-1">Name</label>
        <input
          type="text"
          class="bg-black/40 border border-white/10 p-3 rounded"
        />
      </div>

      <div class="flex flex-col">
        <label class="mb-1">E-Mail</label>
        <input
          type="email"
          class="bg-black/40 border border-white/10 p-3 rounded"
        />
      </div>

      <div class="flex flex-col">
        <label class="mb-1">Nachricht</label>
        <textarea
          rows="6"
          class="bg-black/40 border border-white/10 p-3 rounded"></textarea>
      </div>

      <button
        type="submit"
        class="px-6 py-3 bg-white text-black font-bold rounded hover:bg-sg-primary transition"
      >
        Absenden
      </button>
    </form>
  </main>
</Layout>
